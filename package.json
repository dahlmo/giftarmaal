{
  "name": "giftarmaal",
  "private": true,
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "setup": "node ./scripts/setup.cjs",
    "setup:docker": "node ./scripts/setup-docker.cjs"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}