www.{$DOMAIN} {
  redir https://{$DOMAIN}{uri} permanent
}

{$DOMAIN} {
  encode zstd gzip

  handle /api/* {
    reverse_proxy api:3001
  }

  root * /srv
  file_server

  @notFile {
    not file
    not path /api/*
  }
  rewrite @notFile /index.html
}